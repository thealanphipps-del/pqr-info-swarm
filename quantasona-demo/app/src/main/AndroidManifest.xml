<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required for WebSocket networking -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Required for foreground service notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional: allow worker to restart after reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:windowSoftInputMode="adjustResize"
        android:theme="@style/Theme.PatentDemo">
        <activity
            android:name=".MainActivity"
            android:exported="true">
        </activity>

        <!-- Onboarding entry point -->
        <activity
            android:name="com.swend.mesh.ui.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Connected screen -->
        <activity
            android:name="com.swend.mesh.ui.ConnectedActivity"
            android:exported="false" />

        <!-- Core background worker -->
        <service
            android:name="com.swend.mesh.worker.MobileWorkerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Optional: restart worker after reboot -->
        <receiver
            android:name="com.swend.mesh.worker.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
